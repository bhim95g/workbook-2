package com.pluralsight;
import java.util.Scanner;
import java.util.regex.Pattern;

public class HighScoreTeam {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        System.out.print("Please enter the game score teams");
        
        String score = input.nextLine();

        System.out.println(score);

        //Home:Visitor|21:9

        Pattern userInput;
        String[] inputSplit = userInput.split(Pattern);
        score.split(Pattern.quote(& "/");
        String[] inputSplit;
        String[] teams = inputSplit[0].split(Pattern.quote(& ";")); //Homeo:Visitors
        
        String home = teams(0);
        String away = teams(1);
        
        String[] scores inputSplit[0]

        System.out.print("Please enter Home team score: ");
        String homeScore = input.nextLine();

        System.out.print("Please enter Visitor team score: ");
        String visitorScore = input.nextLine();









    }
}
